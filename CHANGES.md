# AIフィードバックシステム 修正内容

## 主な修正点

### 1. **ユーザーIDとユーザー名の扱いを修正**

**問題点:**
- 元のコードでは、集計シートから「氏名」列を取得して `userId` として扱っていました
- しかし、ユーザーマスターでは `id` と `name` が別々に管理されており、照合に失敗していました

**修正内容:**
- 集計シートから取得するデータの `userId` を `userName` に変更
- ユーザーマスターとの照合を `id` ではなく `name`（氏名）で行うように変更
- グループ化も `userName` で行うように修正

**影響箇所:**
- `getYesterdayData()`: `userId: row[nameIndex]` → `userName: row[nameIndex]`
- `groupDataByUser()`: `const userId = record.userId;` → `const userName = record.userName;`
- `sendDailyFeedback()`: ユーザー検索を `u => u.id === userId` → `u => u.name === userName` に変更

### 2. **Gemini APIのモデル名を修正**

**問題点:**
- `gemini-2.5-flash` というモデルは存在しません

**修正内容:**
- 正しいモデル名 `gemini-1.5-flash` に変更
- APIレスポンスのエラーハンドリングを強化
- レスポンスコードのログ出力を追加

**影響箇所:**
- `callGeminiAPI()`: モデル名を `gemini-1.5-flash` に変更
- エラーメッセージの改善

### 3. **管理者メール送信条件の修正**

**問題点:**
- `if (!adminEmail || adminEmail === 'masatohayashi1977@gmail.com')` という条件で、デフォルト値と同じメールアドレスの場合に送信をスキップしていました
- しかし、CONFIGでデフォルト値として `'masatohayashi1977@gmail.com'` が設定されているため、管理者メールが送信されませんでした

**修正内容:**
- 条件を `if (!adminEmail)` に変更し、メールアドレスが設定されていればそのまま送信するように修正
- エラー通知も同様に修正

**影響箇所:**
- `sendAdminSummary()`: 条件チェックを簡素化
- `sendErrorNotification()`: 条件チェックを簡素化

### 4. **エラーハンドリングの強化**

**追加内容:**
- スタックトレースのログ出力を追加
- シートの存在チェックを追加
- 空データのチェックを追加
- 必須カラムの存在チェックを追加
- APIレスポンスコードの確認を追加

**影響箇所:**
- `sendDailyFeedback()`: スタックトレースのログ追加
- `getYesterdayData()`: シート存在チェック、カラムチェック追加
- `getUserMaster()`: シート存在チェック追加
- `getTypeMaster()`: シート存在チェック追加
- `getCustomerMaster()`: シート存在チェック追加
- `callGeminiAPI()`: レスポンスコードチェック追加

### 5. **所要時間のDate型対応**

**追加内容:**
- Googleスプレッドシートで時刻形式（Date型）として保存されている場合の処理を追加
- `calculateTotalDuration()` と `getMostTimeConsumingTask()` でDate型の処理を追加

**影響箇所:**
- `calculateTotalDuration()`: Date型の処理を追加
- `getMostTimeConsumingTask()`: Date型の処理を追加
- `formatFeedbackEmail()`: Date型の表示処理を追加

### 6. **トリガー設定関数の追加**

**追加内容:**
- 毎日8:00に自動実行するトリガーを設定する関数を追加
- トリガーを削除する関数を追加

**新規関数:**
- `setupDailyTrigger()`: トリガーを設定
- `deleteDailyTrigger()`: トリガーを削除

### 7. **テスト関数の改善**

**追加内容:**
- 今日のデータでテストできる関数を追加
- データ取得のみをテストする関数を追加
- ログ出力を充実

**新規関数:**
- `testFeedbackToday()`: 今日のデータでテスト（メール送信はコメントアウト）
- `testDataRetrieval()`: データ取得のみテスト

### 8. **その他の改善**

- 空行のスキップ処理を追加
- null/undefinedチェックを強化
- ログ出力を充実させてデバッグしやすく改善
- HTML出力の改善（管理者向けサマリーにAI評価を含める）
- 日付の無効値チェックを追加

## 使用方法

### 1. 初回設定

1. Google Apps Scriptエディタにコードをコピー
2. `CONFIG.GEMINI_API_KEY` を自分のAPIキーに変更
3. 必要に応じて `CONFIG.ADMIN_EMAIL` を変更
4. スプレッドシートのシート名が以下と一致することを確認:
   - `集計`
   - `ユーザー管理`
   - `種別`
   - `顧客`

### 2. トリガー設定

スクリプトエディタで `setupDailyTrigger()` 関数を実行すると、毎日8:00に自動実行されるようになります。

```javascript
// 実行方法:
// 1. スクリプトエディタで「setupDailyTrigger」を選択
// 2. 「実行」ボタンをクリック
// 3. 初回実行時は権限の承認が必要
```

### 3. テスト実行

テストする場合は以下の関数を使用:

- `testDataRetrieval()`: データが正しく取得できるかテスト
- `testFeedbackToday()`: 今日のデータでAI評価まで実行（メール送信なし）
- `testFeedback()`: 前日のデータで完全に実行（メール送信あり）

### 4. トリガー削除

自動実行を停止する場合は `deleteDailyTrigger()` 関数を実行します。

## 注意事項

1. **Gemini APIキーの管理**
   - APIキーは公開しないように注意
   - スクリプトプロパティに保存することを推奨

2. **メール送信のテスト**
   - 初めて実行する前に `testFeedbackToday()` でデータ取得とAI評価をテスト
   - メール送信は `testFeedback()` で少量のデータで確認

3. **シート構造**
   - 各シートに必要なカラムが存在することを確認
   - 特に「集計」シートの「氏名」列と「ユーザー管理」シートの「氏名」列が一致していること

4. **日付フォーマット**
   - 「集計」シートの「年月日」列は日付型またはYYYY-MM-DD形式の文字列で保存

5. **所要時間フォーマット**
   - 数値（時間単位）、文字列（"1.5"など）、時刻形式（"1:30"など）、Date型のいずれかに対応

## トラブルシューティング

### メールが送信されない場合

1. ログを確認:
   - `表示` > `ログ` でエラーメッセージを確認

2. ユーザーマスターを確認:
   - 「ユーザー管理」シートにメールアドレスが正しく登録されているか
   - 「氏名」が「集計」シートの「氏名」と完全一致しているか

3. トリガーを確認:
   - `トリガー` メニューでトリガーが設定されているか確認

### AIフィードバックが生成されない場合

1. Gemini APIキーを確認:
   - APIキーが正しいか
   - APIキーの利用制限に達していないか

2. ログでAPIエラーを確認:
   - レスポンスコードとエラーメッセージを確認

### データが取得できない場合

1. `testDataRetrieval()` を実行してログを確認
2. シート名とカラム名が正しいか確認
3. 日付フォーマットが正しいか確認
